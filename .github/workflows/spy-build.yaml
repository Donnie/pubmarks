name: pubmarks/spy build and release

on:
  push:
    branches: [main]
    paths:
      - "scripts/go/spy/**"

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ^1.25.7

      - name: Get previous tag
        id: previoustag
        run: |
          tag=$(gh api "repos/${{ github.repository }}/tags" --jq \
            '[.[] | select(.name | startswith("spy-"))] | .[0].name // "spy-0.0.0"' \
            2>/dev/null || echo "spy-0.0.0")
          version="${tag#spy-}"
          echo "tag=$version" >> $GITHUB_OUTPUT
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Get next minor version
        id: semvers
        uses: WyriHaximus/github-action-next-semvers@v1
        with:
          version: ${{ steps.previoustag.outputs.tag }}

      - name: Build
        working-directory: scripts/go/spy
        run: |
          CGO_ENABLED=0 go build -o spy .
          mkdir -p ../../../out
          mv spy ../../../out/spy

      - name: Create release
        id: create-release
        uses: softprops/action-gh-release@v2.0.8
        with:
          draft: false
          fail_on_unmatched_files: true
          files: out/spy
          generate_release_notes: true
          prerelease: false
          tag_name: "spy-${{ steps.semvers.outputs.patch }}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
